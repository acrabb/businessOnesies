<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    
    
    <!-- Sidebar Themes -->
    <style name="sidebarButtonStyle">
        <item name="android:layout_width">@dimen/sidebarButtonWidth</item>
        <item name="android:layout_height">@dimen/sidebarButtonWidth</item>
        <item name="android:layout_marginBottom">@dimen/sidebarButtonTopBottomMargin</item> 
        <item name="android:scaleType">fitCenter</item>
    </style>
    <style name="sidebarSpacerStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">5dp</item>
        <item name="android:background">@color/achalRed</item>
    </style>

</resources>